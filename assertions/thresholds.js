// Minimum metric score (0–1) that counts as a pass. Change this one value to
// move the bar for every metric (ComponentCoverage, PropCoverage, CustomCssLines).
module.exports = { PASS_THRESHOLD: 0.9 }
